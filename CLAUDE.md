# CLAUDE.md

**Read [AGENTS.md](AGENTS.md) — it is the canonical shared context for this
repository.** Build and test commands, XcodeGen workflow, architecture premises,
the camera/OCR pipeline and its two traps, design tokens, current placeholders,
and the collaboration workflow all live there.

This file is a pointer on purpose. The context is about the project, not about
which agent is reading it, so keeping a second copy here would only let the two
drift apart. If you need to change guidance, change `AGENTS.md`.
