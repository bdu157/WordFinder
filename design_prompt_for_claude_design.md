# Claude Design 전달용 프롬프트 — WordFinder

아래 텍스트를 그대로 Claude Design에 붙여넣으세요.

---

나는 **WordFinder**라는 iOS 앱을 디자인하고 있어. 이 앱은 카메라로 모르는 영단어를 찍으면 즉시 뜻·예문·발음을 보여주는 **영영사전 앱**이야.

## 제품 컨텍스트

- **한 줄 정의**: 원서·논문·표지판에서 모르는 영단어를 카메라로 찍으면 3초 안에 영영 정의·품사·예문·발음을 보여주는 앱.
- **타깃 사용자**: 영어 원서/전공서를 읽는 중상급 학습자, 유학생, 해외 체류자. 이미 어느 정도 영어를 하는 사람들이라 유아적이거나 과하게 캐주얼한 톤은 피해야 해.
- **핵심 차별점**: 이건 번역기가 아니라 **사전**이야. Google 렌즈나 Papago처럼 문장을 통째로 번역해주는 게 아니라, 찍은 범위 안의 단어를 개별적으로 인식해서 **진짜 사전처럼** 정의·품사·예문·발음기호를 보여줘. 그리고 찾아본 단어가 문맥 문장과 함께 학습 히스토리에 쌓여. 이 "번역기가 아니라 사전"이라는 정체성이 디자인에도 드러났으면 해 — 가볍고 즉흥적인 유틸 앱이 아니라, **신뢰할 수 있는 참고 도구(reference tool)**처럼 느껴져야 해.

## 비주얼 컨셉

- **톤앤매너**: 미니멀 · 학구적(academic). 종이 사전, 도서관, 대학 노트 같은 느낌을 은은하게 참조하되 복고풍으로 가지는 않았으면 해. 절제되고 신뢰감 있는 톤.
- **메인 브랜드 컬러**: 블루/네이비 계열. 신뢰감·집중력을 주는 방향으로.
- **무드 키워드**: 차분함, 신뢰, 정밀함, 여백, 활자(typography) 중심, 절제된 하이라이트.
- **피해야 할 것**: 만화 같은 일러스트, 형광색·그라디언트 남발, "AI 스타트업스러운" 보라/네온 톤, 지나치게 장난스러운 마이크로카피나 캐릭터.
- **타이포그래피 제안**: iOS 네이티브 폰트를 기준으로, 표제어와 정의문에는 세리프(Apple의 New York 등)를 포인트로 써서 "진짜 사전을 펼친" 느낌을 주고, 나머지 UI 크롬(버튼·탭바·라벨)은 San Francisco 계열 산세리프로 대비를 주는 방향을 제안해줘. 이 조합이 어색하면 대안도 제시해줘.

## 플랫폼 제약 (중요)

이 앱은 **iOS 네이티브(SwiftUI)** 로 개발돼. 따라서:
- iOS Human Interface Guidelines를 따르는 디자인이어야 해 (네이티브 탭바, 네비게이션 바, SF Symbols 아이콘 스타일, Dynamic Type 대응).
- 웹이나 안드로이드 스타일 컴포넌트가 아니라 **iOS 네이티브 컴포넌트로 그대로 구현 가능한 디자인**으로 제안해줘.
- 라이트 모드와 다크 모드 둘 다 필요해. 네이비 톤이 다크 모드에서도 위계·대비가 살도록 컬러 시스템을 설계해줘.

## 화면 구조 (탭 3개, 홈 화면 없음)

이 앱은 별도의 "홈" 화면이 없어. **카메라 화면이 곧 앱의 시작 화면**이고, 앱을 실행하면 바로 카메라가 켜져.

탭바 구조: **카메라(시작 화면) / 히스토리 / 설정** — 3개 탭.

1. **카메라 (시작 화면)**
   - 앱 실행 시 바로 진입, 카메라 프리뷰가 즉시 활성화됨. 검색바나 다른 진입점 없이 이게 첫 화면이야.
   - iPhone 카메라 앱이 초점/노출 영역을 강조하는 것처럼, **화면에는 인식 활성 구역 하나만 선명하게 보이고 그 바깥 전체는 블러(비네팅/스포트라이트 효과)** 처리돼.
   - 활성 구역은 **고정 크기의 가로로 긴 텍스트 박스**야: 가로 폭은 iPhone 화면 가로 길이의 절반, 세로 높이는 10pt 텍스트 한 줄이 들어가는 비율 — 즉 한두 줄짜리 얇고 긴 슬릿 형태이고, 화면 정중앙에 고정으로 위치해. 사용자가 드래그로 크기·위치를 바꾸는 게 아니라, **폰을 움직여서 원하는 텍스트를 이 박스 안에 맞추는 방식**이야.
   - 인식된 단어의 뜻은 이 카메라 화면 위에 **바텀시트(bottom sheet)나 오버레이 카드**로 떠서 보여줄 걸 상정하고 있어 — 화면 전환 없이 카메라 화면 위에서 바로 결과(표제어, 발음기호, 품사별 정의, 예문, 발음 재생 버튼)를 확인하는 흐름이야.

2. **히스토리**
   - 지금까지 검색한 단어들의 리스트.
   - 상단에 검색바가 있어서 히스토리 안에서 특정 단어를 검색할 수 있어.

3. **설정**
   - **테마 선택(라이트 모드 / 다크 모드)** 이 핵심 항목이야. 시스템 설정을 따라가는 게 아니라 앱 안에서 명시적으로 선택하는 토글/세그먼트 컨트롤이 필요해.

> 참고: 이전에 고려했던 "홈", "즐겨찾기" 탭과 "인식 결과"/"단어 상세"라는 별도 화면은 이번 3탭 구조에서 빠졌어. 지금은 이 3개 화면(카메라/히스토리/설정)에 집중해줘.

## 요청사항

다음을 만들어줘:
1. **컬러 시스템**: 라이트/다크 모드 각각의 primary(네이비 계열)·accent·background·surface·text 색상 팔레트. 실제 hex 값으로.
2. **타이포그래피 스케일**: 표제어/본문/캡션 등 텍스트 스타일별 폰트·크기·굵기 제안 (세리프+산세리프 조합 포함).
3. **핵심 화면 목업**: 카메라 화면(블러 배경 + 고정 텍스트 박스 가이드 + 결과 바텀시트가 뜬 상태 포함), 히스토리, 설정(테마 토글 포함) — 이 3개 전부.
4. **컴포넌트 가이드**: 카메라 화면의 텍스트 박스 가이드/블러 비네팅, 결과 바텀시트 카드, 발음 재생 버튼(🔊), 히스토리 검색바, 테마 선택 컨트롤 같은 이 앱 특유의 UI 요소 스타일.
5. **앱 아이콘 방향성**: 위 컨셉에 맞는 아이콘 컨셉 2~3가지 제안 (예: 카메라 뷰파인더 + 사전 표제어를 결합한 모티프 등).

일단 **카메라 화면(블러 + 텍스트 박스 가이드 + 결과 바텀시트)** 부터 만들어서 톤이 맞는지 같이 확인해보고, 맞으면 히스토리·설정으로 확장하자.
